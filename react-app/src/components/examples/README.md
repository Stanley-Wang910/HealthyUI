a couple of material ui components as examples to get started 

see https://mui.com/material-ui/all-components/   
for full list of component  

material uses emotion CSS by default 
however we have it configured so that you can also use tailwind if you want 
try to use default MUI styles, there's probably not a great reason you need to customize the CSS with TW 



import * as React from 'react'
import Button from '@mui/material/Button'

const ButtonWrapper = () => {
  return (
    <Button variant="contained" className="btn btn-red">
      Hello I am an MUI button
    </Button>
  )
}

export default ButtonWrapper
